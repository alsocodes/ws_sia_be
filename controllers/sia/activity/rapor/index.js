const { formOpt } = require("./form-opt");
const { get } = require("./get");
const { bulkRaporUpload } = require("./bulk-rapor-upload");
const { update } = require("./update");

module.exports = { formOpt, get, bulkRaporUpload, update };